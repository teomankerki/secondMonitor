<UserControl x:Class="MegaApp.Modules.ParticleFlowModule"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="240" d:DesignWidth="260">
    <Border Style="{StaticResource ModuleCard}" Background="Transparent">
        <Grid>
            <TextBlock Text="Ambient Drift" Style="{StaticResource ModuleTitle}" />
            <Canvas x:Name="ParticleCanvas" Margin="0 40 0 0" ClipToBounds="True" />
        </Grid>
    </Border>
</UserControl>

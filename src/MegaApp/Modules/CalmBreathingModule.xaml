<UserControl x:Class="MegaApp.Modules.CalmBreathingModule"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="240" d:DesignWidth="260">
    <Border Style="{StaticResource ModuleCard}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid>
                <Ellipse Width="120" Height="120" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Ellipse.Fill>
                        <RadialGradientBrush GradientOrigin="0.3,0.3">
                            <GradientStop Color="#337FD1AE" Offset="0" />
                            <GradientStop Color="#117FD1AE" Offset="1" />
                        </RadialGradientBrush>
                    </Ellipse.Fill>
                    <Ellipse.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard RepeatBehavior="Forever">
                                    <DoubleAnimation Storyboard.TargetProperty="Width" To="160" Duration="0:0:4" AutoReverse="True" />
                                    <DoubleAnimation Storyboard.TargetProperty="Height" To="160" Duration="0:0:4" AutoReverse="True" />
                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0.8" To="0.4" Duration="0:0:4" AutoReverse="True" />
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Ellipse.Triggers>
                </Ellipse>
                <TextBlock Text="Inhale" x:Name="PhaseText" HorizontalAlignment="Center" VerticalAlignment="Center"
                           FontSize="20" Foreground="#F7FBFF" />
            </Grid>
            <TextBlock Grid.Row="1" Text="Follow the breathing rhythm" FontSize="12" Foreground="#AAFFFFFF" HorizontalAlignment="Center" Margin="0 10 0 0" />
        </Grid>
    </Border>
</UserControl>
